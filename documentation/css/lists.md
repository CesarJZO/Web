# Lists

## Ordered list

```css
ol {
    padding-left: 0px;
    list-style-type: none;
    list-style-position: inside;
}
```

A list can be shown within the same line

```css
li {
    display: inline;
}
```
