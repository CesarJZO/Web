# Pseudo classes

It's a keyword added to a selector that specifies a special state of the selected element

```css
a:hover {
  color: blue;
}
```

```css
a:active {

}
```

```css
a:focus {
  
}
```
